<?xml version="1.0" encoding="utf-8"?>
<!--
    Solid white pill behind the home screen search bar. Opaque rather than a tint over the
    wallpaper: the hint text is dark, and dark-on-translucent is unreadable over a light
    wallpaper. The radius only has to beat half the 44dp height to round the ends fully.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <solid android:color="@android:color/white" />

    <corners android:radius="24dp" />

</shape>
